@model eLogin.Models.EntityCategory

@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Create</h1>

<h4>EntityCategory</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="CreateCategory">
            <div asp-valIdation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="CategoryName" class="control-label"></label>
                <input asp-for="CategoryName" class="form-control" />
                <span asp-valIdation-for="CategoryName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ParentEntityCategoryId" class="control-label"></label>
                @*<input asp-for="ParentEntityCategoryId" class="form-control" />*@
                @*<select asp-for="ParentEntityCategoryId" asp-items="@ViewData["Categories"] as List<SelectListItem>"></select>*@
                <ejs-dropdownlist id="ParentEntityCategoryId" dataSource="@ViewBag.Categories" placeholder="Select an Entity Category">
                    <e-dropdownlist-fields text="Text" value="Value"></e-dropdownlist-fields>
                </ejs-dropdownlist>
                <span asp-valIdation-for="ParentEntityCategoryId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
